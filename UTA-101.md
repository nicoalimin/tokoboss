# UTA-101: Drizzle TransferStore Header Draft/Read Only

## Acceptance Criteria Checklist

1. ✅ Create fresh branch `devbox/uta-101-5` from current main
2. ✅ Implement `DrizzleCatalogStore` with `TransferStore` interface
3. ✅ Implement `createTransferDraft` method
4. ✅ Implement `findTransferById` method
5. ✅ Implement `listTransfers` method
6. ✅ Add stubs for unimplemented TransferStore methods with proper errors
7. ✅ Create comprehensive tests in `transfers-drizzle-header.test.ts`
8. ✅ Verify pnpm typecheck passes
9. ✅ Verify pnpm --filter @tokoboss/database test passes
10. ✅ Verify git diff shows only allowlisted paths
11. ✅ Verify no forbidden paths were modified

## Implementation Status

### Current Progress

- [x] Branch creation
- [x] Repository analysis
- [x] Implementation
- [ ] Testing
- [ ] Verification

## Key Requirements

**Allowlist (must modify):**

- `packages/database/src/repositories/drizzle-catalog-repository.ts`
- `packages/database/src/__tests__/` (new test file)

**Forbidden (never modify):**

- `packages/application/**`
- `infra/drizzle/**`
- `packages/database/src/schema/**`
- `pnpm-lock.yaml`
- `web/`, `mobile/`, etc.

**TransferStore Methods to Implement:**

1. `createTransferDraft` - Create draft transfer with validation
2. `findTransferById` - Find transfer by ID (null across workspaces)
3. `listTransfers` - List transfers scoped to workspace
4. `addTransferItems` - Stub only (throw 'UTA-102')
5. `findTransferWithItems` - Stub only (throw 'UTA-102')
6. `listTransfersWithItems` - Stub only (throw 'UTA-102')

## Testing Requirements

Tests must cover:

- ✅ Draft creation success
- ✅ Duplicate referenceNum conflict (catalogConflict)
- ✅ Cross-workspace isolation
- ✅ ListTransfers scoped and oldest-first ordering
- ✅ Real assertions (no expect(true))

## Verification Commands

```bash
pnpm typecheck
pnpm --filter @tokoboss/database test
git diff --name-only origin/main
```

## Notes

- Do NOT reuse branches UTA-101-1 through UTA-101-4
- Hard rule: only modify allowlisted paths
- Follow migration rebase rules if conflicts occur
- PR will be opened when all criteria met and tests pass
